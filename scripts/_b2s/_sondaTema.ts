/** Sonda gramatical por TEMA, sobre un JSON de historias (antes de guardar) o la base. Misma vara que _gramProbe. */
import { readFileSync } from "fs";
const RASGOS: [string, RegExp][] = [];
(async () => {})();
