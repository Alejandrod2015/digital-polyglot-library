---
title: "Hello, MDX"
slug: "hello-world"
date: "2026-05-16"
excerpt: "Quick sanity post to verify the new blog framework renders MDX, frontmatter, headings, lists, and code blocks the way we want."
author: "Digital Polyglot"
tags: ["meta", "blog"]
---

This is a placeholder post used to verify that the new blog framework renders
MDX content correctly. The real posts will replace this one in the next phase.

## What this post tests

A few things we want to make sure look right before importing real content:

- Headings sit at the right size and weight
- Body copy stays readable on the dark background
- Lists, both bullet and numbered, line up
- Inline `code` and code blocks render with the right styling
- Links pick up the gold accent on hover

## A short code block

```ts
function greet(name: string) {
  return `Hello, ${name}!`;
}
```

## A short numbered list

1. Markdown plus React components
2. Statically rendered at build time
3. Lives in git, no external CMS

That is it. Real content lands in phase 2.
